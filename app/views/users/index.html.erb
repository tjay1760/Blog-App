<% if @users.empty? %>
  No users
<% else %>
<h1 class="user-heading"> List of users</h1>
  <% @users.each do |user| %>
    <a href="<%= user_path(user) %>"><%= render user %></a>
    <br>
  <% end %>
<% end %>
