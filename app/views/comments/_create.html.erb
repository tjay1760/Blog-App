<%= form_with model: [@user, @post, @comment], url: user_post_comments_path(@user, @post), class: "form", scope: :comment do |f| %>
  <section class="form-section">
    <%= f.text_area :text,
    placeholder: "Write a comment...", 
    required: true,
    class: "text_input" %>
  </section>

  <section class="form-section">
    <%= f.submit "Comment", class: "submit_button" %>
  </section>
<% end %>
